// Aqui é o arquivo para colocar todo tipo de css mesmo Global ou não.
// Import para cá.

import '../styles/globals.css'
import '../styles/app.css'

function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />
}

export default MyApp
