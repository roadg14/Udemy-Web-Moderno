let preco = 20.9;
let desconto = 0.5;
let produto = "Caderno";

let precoComDesconto = preco * (1 - desconto);

console.log(produto + " custa " + preco + ", mais com o desconto de " + desconto + " fica no valor de " + precoComDesconto);

/////////////////

let preco1 = 19.90;
let desconto1 = 0.5;
let precoComDesconto1 = preco1 * (1 - desconto1)
console.log(precoComDesconto1)

////////////////

let nome = "Douglas"
let sobrenome = "Rodrigues"
console.log(nome + " " + sobrenome);