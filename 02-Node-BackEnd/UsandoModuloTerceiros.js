const _ = require("lodash") // Instalação do NodeMon.
setInterval(() => console.log(_.random(100, 1000)), 2000); // Isso aqui é uma forma de sorteio, tipo entre funcionarios. chama quantos e sorteia.
